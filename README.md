# How to run
``` bash
git clone https://github.com/nurrasyid14/hadoop1.git #if doesn't yet have cloned
# git pull origin main # if have cloned
chmod +x run.sh
chmod +x verificator.sh

./run.sh
./verificator.sh
```
